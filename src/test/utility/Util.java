package utility;

import java.time.Duration;

public class Util {

    public static long PAGE_LOAD_TIMEOUT = 20;
    public static long IMPLICIT_WAIT = 15;
    public static long POLLING = 100;
    public static Duration EXPLICIT_WAIT = Duration.ofSeconds(15000);
}
